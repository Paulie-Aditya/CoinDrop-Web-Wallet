VITE_API_URL="http://localhost:5000"
# When true, the app serves mock data and fakes the Discord login so the UI
# runs with no backend. Set to "false" (or remove) to hit VITE_API_URL.
VITE_USE_MOCK="true"
