# Base URL of the CoinDrop wallet backend (auth + wallet endpoints).
VITE_API_URL="http://localhost:5000"

# "true"  -> mock layer: fake data, fake Discord login, no backend needed.
# "false" -> real requests to VITE_API_URL with cookies (credentials: include).
VITE_USE_MOCK="true"
